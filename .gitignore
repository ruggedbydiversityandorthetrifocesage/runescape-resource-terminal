node_modules/
__pycache__/
.DS_Store
runs/
gateway/agent-state/
screenshots/
public-bot/
public-client/

server.json
.env

# Nix
result
.direnv/

# Bot folders (except template)
bots/*/
!bots/_template/

# SDK is copied into Docker via Dockerfile; don't duplicate it under server/
server/sdk/
jobs/

# RST runtime data — never overwrite live player data on deploy
server/engine/data/rst-*.json